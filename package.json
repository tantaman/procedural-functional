{
  "name": "procedural-functional",
  "scripts": {
    "build": "browserify src/main.js -t babelify -o dist/bundle.js",
    "watch": "npm-watch",
    "test": "jest",
    "pwatch": "./node_modules/onchange/dist/bin.js 'src/**/*.js' -- prettier --write {{changed}}"
  },
  "dependencies": {},
  "watch": {
    "build": "src/**"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "@babel/preset-flow": "^7.10.1",
    "babelify": "^10.0.0",
    "flow-bin": "^0.126.1",
    "jest": "^26.0.1",
    "onchange": "^7.0.2",
    "prettier": "2.0.5"
  }
}
